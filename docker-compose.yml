version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: instory_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: instory
      POSTGRES_PASSWORD: instory_pass
      POSTGRES_DB: instory_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: